@model MvcMovie.Models.Employe

@{
    ViewData["Title"] = "👤 Détails de l'employé";
}

<div class="container mt-4">

    <h2 class="mb-4 text-white">@ViewData["Title"]</h2>

    <div class="card bg-dark text-white shadow-sm p-4 mb-4 border-light">
        <dl class="row">
            <dt class="col-sm-3 fw-bold">🆔 Identifiant</dt>
            <dd class="col-sm-9">@Model.Id</dd>

            <dt class="col-sm-3 fw-bold">👤 Nom</dt>
            <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.Nom) ? "ℹ️ Non renseigné" : Model.Nom)</dd>

            <dt class="col-sm-3 fw-bold">👤 Prénom</dt>
            <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.Prenom) ? "ℹ️ Non renseigné" : Model.Prenom)</dd>

            <dt class="col-sm-3 fw-bold">📧 Email</dt>
            <dd class="col-sm-9">@Model.Email</dd>

            <dt class="col-sm-3 fw-bold">📅 Date d'embauche</dt>
            <dd class="col-sm-9">@Model.DateEmbauche.ToString("dd/MM/yyyy")</dd>
        </dl>
    </div>

    <div>
        
        <a asp-action="Index" class="btn btn-secondary">
            ⬅️ Retour
        </a>
    </div>
</div>
